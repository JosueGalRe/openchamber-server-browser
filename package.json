{
  "name": "@josuegalre/openchamber-server-browser",
  "version": "0.1.0",
  "private": true,
  "description": "Server-owned Chrome browser provider and shared surface for OpenChamber",
  "type": "module",
  "license": "MIT",
  "repository": "github:JosueGalRe/openchamber-server-browser",
  "engines": {
    "node": ">=22"
  },
  "openchamber": {
    "apiVersion": 1,
    "engines": {
      "openchamber": ">=1.24.2"
    },
    "contributes": {
      "panel": {
        "id": "server-browser",
        "name": "Server Browser",
        "icon": "global"
      },
      "service": {
        "entry": "service/main.js",
        "runtime": "host",
        "provides": [
          "browser"
        ],
        "surface": true,
        "permissions": {
          "exec": [
            "chromium",
            "google-chrome"
          ]
        }
      }
    }
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test test/*.test.js",
    "start": "node service/main.js",
    "check": "node --check service/main.js",
    "package": "node scripts/package.mjs"
  },
  "dependencies": {
    "@openchamber/sdk": "file:vendor/openchamber-sdk-56f33fd59.tgz",
    "ws": "8.18.3"
  },
  "devDependencies": {
    "esbuild": "0.28.0"
  }
}
